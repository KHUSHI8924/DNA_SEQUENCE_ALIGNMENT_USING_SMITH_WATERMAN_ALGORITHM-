# LOCAL_DNA_SEQUENCE_ALIGNMENT_USING_SMITH_WATERMAN_ALGORITHM-
Smith–Waterman algorithm performs local alignment, meaning it finds the best matching subsequences between two DNA sequences instead of aligning the entire sequences. It uses dynamic programming to compute a scoring matrix and then performs traceback to obtain the optimal local alignment.
